<ul>
    <% @tasks.each do |task| %>
        <li>
        <%= link_to task.name, task_path(task) %>
        <%= task.status_description %>
        <%= button_to "Complete", toggle_status_path(task) %>
        </li>
    <% end %>
</ul>

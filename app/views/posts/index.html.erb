<ul>
    <% @posts.each do |post| %>
        <li>
            <%# link_to post.content, post_path(post) %>
            <%= post.content %> - <%= post.user.nickname %>
            <% if post.user = current_user %>
                <%= link_to "Edit", edit_post_path(post) %>
                <%= link_to "Delete", post_path(post), method: "delete" %>
            <% end %>
        </li>
    <% end %>
</ul>

<%= render 'form' %>